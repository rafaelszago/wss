# Shell script experiments

Some experiments with Shell Script

## Running commands

Give execute permission to script:

```bash
chmod +x command.sh
```

Add script to source:

```bash
source command.sh
```

And to run script:

```bash
command <params>
```
